import { Injectable } from "@angular/core";
// import { Connect } from "ngrx-action-bundles";
// import { listingListBundles } from "@app/listing/+store/bundles/listing-list";
// import { listingListSelectors } from "@app/listing/+store/selectors";

@Injectable()

export class ListingListModel {
  // actions = this.connect.connectBundles(listingListBundles);
  // selectors = this.connect.connectSelectors(listingListSelectors);
  // constructor(private connect: Connect) {}
}
